# Configuration de TEST — versionnée (aucun secret réel).
# LLM et embeddings sont mockés dans les tests : ces valeurs ne sont pas utilisées en réseau.

POSTGRES_USER=rag
POSTGRES_PASSWORD=rag
POSTGRES_DB=financialrag_test

MISTRAL_API_KEY=test-key-not-used
MISTRAL_MODEL=mistral-small-latest

# OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_BASE_URL=http://localhost:11434
EMBEDDING_MODEL=bge-m3

LLM_PROVIDER=mistral
LOG_LEVEL=WARNING
